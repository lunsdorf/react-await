{
  "name": "react-await",
  "version": "1.0.0",
  "license": "MIT",
  "author": "Sören Lünsdorf <code@lunsdorf.com>",
  "main": "./lib/react-await.js",
  "typings": "./lib/react-await.d.ts",
  "scripts": {
    "build": "rimraf ./lib && npm run lint && tsc -p ./tsconfig.json",
    "lint": "tslint -p ./tsconfig.json -c ./tslint.json ./src/**/*.ts*",
    "prepublish": "npm run build"
  },
  "peerDependencies": {
    "react": "^16.4.0"
  },
  "devDependencies": {
    "@types/react": "^16.3.14",
    "rimraf": "^2.6.2",
    "tslint": "^5.10.0",
    "typescript": "^2.8.3"
  }
}
